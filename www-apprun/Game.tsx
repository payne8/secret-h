import app, { Component, on } from 'apprun';

export default class GameComponent extends Component {
  state = {

  };

  view = (state) => {
    return <div>

    </div>
  }

  update = {
    '#Game': state => state,
  }
}
